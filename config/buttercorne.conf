# Buttercorne BLE keyboard config
CONFIG_ZMK_KEYBOARD_NAME="Buttercorne"

# BLE output
CONFIG_ZMK_BLE=y

# USB output (active when cable is plugged in)
CONFIG_ZMK_USB=y

# USB logging — uncomment to debug via: picocom /dev/ttyACM0 -b 115200
# CONFIG_ZMK_USB_LOGGING=y
